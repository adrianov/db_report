source 'https://rubygems.org'

gem 'activerecord', '~> 7.0'
gem 'pg', '~> 1.5'       # PostgreSQL adapter
# gem 'mysql2', '~> 0.5'   # MySQL adapter, comment if not needed
gem 'sqlite3', '~> 1.6'  # SQLite adapter, comment if not needed

# Optional adapters - uncomment as needed
# gem 'oracle_enhanced', '~> 7.0'  # Oracle adapter
# gem 'sqlserver', '~> 7.0'        # SQL Server adapter

# Required for time parsing
gem 'time'

# Optional utility gems
gem 'colorize', '~> 0.8'         # For colored terminal output
gem 'terminal-table', '~> 3.0'   # For formatted console tables
gem 'progress_bar', '~> 1.3'     # For progress indicators when processing large tables
